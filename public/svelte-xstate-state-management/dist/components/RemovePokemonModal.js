/* src/components/RemovePokemonModal.svelte generated by Svelte v3.31.2 */
import {
	SvelteComponent,
	add_flush_callback,
	bind,
	binding_callbacks,
	bubble,
	create_component,
	destroy_component,
	detach,
	element,
	init,
	insert,
	mount_component,
	safe_not_equal,
	transition_in,
	transition_out
} from "../../web_modules/svelte/internal.js";

import { getContext } from "../../web_modules/svelte.js";
import { Modal } from "../../web_modules/carbon-components-svelte.js";

function create_default_slot(ctx) {
	let p;

	return {
		c() {
			p = element("p");
			p.textContent = "Are you sure you want to remove the selected pokemon?";
		},
		m(target, anchor) {
			insert(target, p, anchor);
		},
		d(detaching) {
			if (detaching) detach(p);
		}
	};
}

function create_fragment(ctx) {
	let modal;
	let updating_open;
	let current;

	function modal_open_binding(value) {
		/*modal_open_binding*/ ctx[2].call(null, value);
	}

	let modal_props = {
		danger: true,
		modalHeading: "Remove Pokemon",
		primaryButtonText: "Remove",
		secondaryButtonText: "Cancel",
		$$slots: { default: [create_default_slot] },
		$$scope: { ctx }
	};

	if (/*open*/ ctx[0] !== void 0) {
		modal_props.open = /*open*/ ctx[0];
	}

	modal = new Modal({ props: modal_props });
	binding_callbacks.push(() => bind(modal, "open", modal_open_binding));
	modal.$on("click:button--secondary", /*click_button_secondary_handler*/ ctx[3]);
	modal.$on("close", /*close_handler*/ ctx[4]);
	modal.$on("submit", /*onSubmit*/ ctx[1]);
	modal.$on("submit", /*submit_handler*/ ctx[5]);
	modal.$on("open", /*open_handler*/ ctx[6]);

	return {
		c() {
			create_component(modal.$$.fragment);
		},
		m(target, anchor) {
			mount_component(modal, target, anchor);
			current = true;
		},
		p(ctx, [dirty]) {
			const modal_changes = {};

			if (dirty & /*$$scope*/ 128) {
				modal_changes.$$scope = { dirty, ctx };
			}

			if (!updating_open && dirty & /*open*/ 1) {
				updating_open = true;
				modal_changes.open = /*open*/ ctx[0];
				add_flush_callback(() => updating_open = false);
			}

			modal.$set(modal_changes);
		},
		i(local) {
			if (current) return;
			transition_in(modal.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(modal.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			destroy_component(modal, detaching);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { open = false } = $$props;

	function onSubmit() {
		$$invalidate(0, open = false);
	}

	function modal_open_binding(value) {
		open = value;
		$$invalidate(0, open);
	}

	const click_button_secondary_handler = () => $$invalidate(0, open = false);

	function close_handler(event) {
		bubble($$self, event);
	}

	function submit_handler(event) {
		bubble($$self, event);
	}

	function open_handler(event) {
		bubble($$self, event);
	}

	$$self.$$set = $$props => {
		if ("open" in $$props) $$invalidate(0, open = $$props.open);
	};

	return [
		open,
		onSubmit,
		modal_open_binding,
		click_button_secondary_handler,
		close_handler,
		submit_handler,
		open_handler
	];
}

class RemovePokemonModal extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { open: 0 });
	}
}

export default RemovePokemonModal;